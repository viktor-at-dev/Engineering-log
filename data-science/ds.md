[Check out my understanding of the data life cycle](../data-analytics/data-ingestion-validation.md)
**solving_package_issues**:poor internet can cause os to get confused leading to packages being improperly installed
run this command `curl -C`: this tells the os to continue installing even after but it is mainly due to the -C flag that tells the os to continue from its point of disruption, a flag of curl-o which an operation can be cancalled due to reasonswould be telling the os to start installing from scratch. Curl basically writes to the hardisk in append mode giving more access to addition
disconnection upon recovery
If you want to extend the time with  such as poor net run
 ```conda config --set remote_read_timeout_secs 120
conda config --set remote_connect_timeout_secs 30
conda config --set remote_backoff_factor 2
```

